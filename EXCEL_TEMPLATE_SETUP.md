# 병원 양식으로 엑셀 출력하기

## 적용

ZIP에는 HTML·CSS·JS, 엑셀 템플릿, vendor 라이브러리와 검증 코드가 모두 포함되어 있습니다. 압축을 풀면 nurse-scheduler 폴더 자체가 전체 제품입니다. 이전 배포 파일을 따로 조합할 필요가 없습니다.

GitHub Pages 또는 로컬 HTTP 서버에서 index.html을 열고 **엑셀 출력**을 누르세요. HTML을 직접 더블클릭하는 file:// 방식은 템플릿을 읽을 수 없습니다. 사이트 실행에 npm 설치나 외부 CDN은 필요하지 않습니다.

Docker가 설치되어 있다면 압축을 푼 폴더에서 PowerShell로 다음을 실행합니다.

```powershell
docker run --rm -d --name nurse-scheduler -p 8080:80 --mount "type=bind,source=$($PWD.Path),target=/usr/share/nginx/html,readonly" nginx:alpine
```

http://localhost:8080 에 접속합니다. 기존 컨테이너가 같은 이름으로 실행 중이라면 기존 실행 환경을 그대로 사용하세요.

템플릿은 GitHub에 올라간 nurse_scheduler_template.xlsx를 포함했습니다. 새 폴더를 배포할 때 폴더 안의 파일들을 모두 함께 사용하세요. 저장된 근무표는 브라우저의 동일한 주소에서 유지됩니다.

## 양식 기준

- 업로드된 병원 원본의 셀 위치를 사용합니다. 날짜는 D7:AH7, 요일은 D8:AH8, 직원은 9행부터입니다.
- **직책칸은 B열의 내용만 비우세요.** B열 자체를 삭제해 날짜 열을 이동한 양식은 지원하지 않습니다.
- 여러 월 시트가 들어 있는 경우 왼쪽부터 첫 번째 일반 근무표를 사용합니다. 이름에 `정기`가 들어간 시트는 건너뜁니다. 특정 양식으로 고정하려면 그 일반 근무표를 가장 왼쪽에 놓으세요.
- 간호사와 AN 사이 이중선, 하단 D/E/N/M 집계 4행은 유지하세요. 지워진 E/N/M 집계 항목명은 출력할 때 채웁니다.
- 다운로드에는 해당 월 근무표만 포함합니다. 기존 기능처럼 편성 문제가 있으면 `편성 확인` 시트도 함께 생성합니다.
- 템플릿 파일 자체는 수정하지 않습니다. 다운로드 파일의 제목·연월·날짜·요일·직원 영역·집계·인쇄 범위만 갱신합니다.

## 직원 배치와 인쇄

- 상단: HN/RN/MD/NK 간호사. 직책별 재정렬 없이 관리 목록에서 지정한 순서를 유지합니다.
- 하단: AN. 두 구역 사이에 이중선을 넣습니다.
- 린넨은 직원 데이터에 있더라도 출력하지 않습니다. 원본 린넨 행도 직원 영역을 다시 채울 때 제외합니다.
- 직원이 있는 구역에 여유 행을 각각 1개 둡니다. 원본 수용 행 수를 넘으면 먼저 여유 행을 없앱니다.
- 더 많은 인원은 행을 늘립니다. 열 너비와 직원 행 높이는 원본 기준을 유지하며, A4 가로 너비 1페이지로 설정합니다. 원본 수용 인원을 넘는 경우 세로는 필요한 만큼 페이지를 나눕니다.
- 우측 인사 기록란까지 포함해 실제 표 범위를 인쇄 영역으로 지정합니다. 다중 페이지에는 제목과 날짜 머리글(1~8행)을 반복합니다.
- 실제 용지 출력 크기는 Excel 버전과 프린터에 따라 다르므로 첫 출력은 인쇄 미리보기에서 확인하세요.

## 근무와 합계

- 사이트의 최종 배정값을 사용합니다. 원티드·배정 데이터·스냅샷은 수정하지 않습니다.
- 사이트의 `O`는 병원 표기인 `OFF`로 출력합니다. 미배정 날짜도 현재 사이트 규칙대로 OFF로 출력합니다.
- 휴가는 `휴가`로 출력하며 연차 사용으로 단정하지 않습니다.
- 일반 근무와 일반 OFF는 흰색입니다. 주말·공휴일은 템플릿의 노란색을 적용합니다.
- 직접 입력한 원티드 D/E/N/M과 휴가는 짙은 파란색(흰 글씨)이며 휴일 배경보다 우선합니다.
- 직접 지정한 OFF는 주말·공휴일에도 흰색입니다. 날짜 머리글의 노란색은 유지합니다.
- 배경색은 다운로드 시점의 원티드 정보를 기준으로 셀에 저장합니다. Excel에서 나중에 내용을 수정해도 배경이 자동으로 바뀌지는 않습니다.
- 개인별 N 횟수와 OFF 일수는 수식으로 계산합니다. `연` 열은 수기 입력된 `연` 또는 `연차`만 계산합니다. `연1`처럼 번호가 붙은 표기와 반차·특수휴가는 아직 자동 집계하지 않습니다.
- 원본 구분을 따라 하단 D/E/N은 상단 간호사만, M은 간호사와 AN을 합산합니다.
- 여유 행에도 개인별 집계 수식이 있습니다. 이름을 넣으면 집계가 표시됩니다.
- `기준 OFF`의 수치는 사이트에 대응 정보가 없어 비워둡니다. 구연차·신연차·잔여연차·입사일·미오프·사인 등도 수기 기록란으로 남깁니다.
- 공휴일 표시는 사이트의 해당 연도 공휴일 목록을 사용합니다.

## 검증

Node.js가 있는 개발 환경에서만 다음 명령을 실행합니다. 사이트 이용자에게는 필요 없습니다.

```bash
npm install
npm test
```

기본 검증 파일은 프로젝트 루트의 `nurse_scheduler_template.xlsx`입니다. 다른 경로를 쓸 경우 환경 변수 `TEMPLATE_FILE`로 지정합니다. 검증에는 그룹 분리, 이중선, 원본 머리글 서식·병합·여백 보존, 28~31일, 27명 확장, 직책 글자·린넨 행 삭제, 집계, 잘못된 파일 처리, 출력 중 월 변경 방지가 포함됩니다.

기존 자동생성 규칙·localStorage 데이터 구조는 변경하지 않습니다. 엑셀 가져오기와 직원 ID/출력 스냅샷의 파일 내 저장은 이번 작업에 포함하지 않습니다.

배포 버전: 20260923-roster2. HTML과 엑셀 모듈 URL에 버전값을 사용합니다. 배포 후 페이지를 새로 열어 엑셀을 다시 다운로드하세요. 기존에 다운로드한 엑셀 파일은 바뀌지 않습니다.

이번 달 편성 인원에 포함된 직원만 출력합니다. AN은 하단에 모으며 AN 내부의 목록 순서도 유지합니다.
